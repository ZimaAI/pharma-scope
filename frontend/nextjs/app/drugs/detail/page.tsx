"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import PharmaScopeShell, { StatusBadge } from "@/components/pharma/PharmaScopeShell";
import { demoDrugs, getDrug, Drug } from "@/components/pharma/pharmaApi";
export default function DrugDetailPage() { const [drug, setDrug] = useState<Drug>(demoDrugs[0]); useEffect(() => { const id = new URLSearchParams(window.location.search).get("id") || demoDrugs[0].id; getDrug(id).then(setDrug); }, []); return <PharmaScopeShell title="药物详情" description="对象身份、关联记录与资料更新时间。"><div className="ps-grid-2"><section className="ps-card"><div className="ps-card-head"><div className="ps-detail-title"><div className="ps-drug-badge">{drug.development_code.slice(-3)}</div><div><h2>{drug.display_name}</h2><small>{drug.id}</small></div></div><StatusBadge status="当前档案" tone="success" /></div><div className="ps-card-body"><dl className="ps-kv"><dt>研发代号</dt><dd>{drug.development_code}</dd><dt>研究标签</dt><dd>{drug.indications.join("、")}</dd><dt>靶点</dt><dd>{drug.targets.join("、")}</dd><dt>档案版本</dt><dd>v{drug.revision}</dd><dt>最近更新</dt><dd>{new Date(drug.updated_at).toLocaleString("zh-CN")}</dd></dl><div className="ps-callout amber" style={{ marginTop: 22 }}>档案字段来自工作区同步，未知内容保持为空，不从其他对象推断。</div></div></section><div className="ps-stack"><section className="ps-card"><div className="ps-card-head"><h2>关联记录</h2></div><div className="ps-card-body"><p className="ps-subtle">关联试验和文献数量由服务端返回。</p><Link href="/trials" className="ps-text-link">查看关联试验 →</Link><br /><Link href="/events" className="ps-text-link">查看变化时间线 →</Link></div></section><section className="ps-card"><div className="ps-card-body"><Link href="/research/new" className="ps-btn primary">基于此对象发起研究</Link></div></section></div></div></PharmaScopeShell>; }
